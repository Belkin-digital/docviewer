# Библиотеки разметки — докачиваются при первом запуске
vendor/

# Локальный список проектов этой машины
projects.json

.DS_Store

# Папки этой машины: избранное и добавленные вручную
workspaces.json
